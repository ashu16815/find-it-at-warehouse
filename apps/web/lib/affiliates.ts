export function decorateAffiliate(url: string): string {
  // Skimlinks server-side decoration can be added here when credentials are provided.
  // Until then, return the original URL.
  return url;
}
